includes:
    - ./vendor/nunomaduro/larastan/extension.neon

parameters:

    paths:
        - src
        - tests

    # The level 9 is the highest level
    level: 5

    checkMissingIterableValueType: false

    ignoreErrors:
        -
            message: '#Cannot access property \$url_notify on array#'
            path: tests/*
